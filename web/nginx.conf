server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Angular router uses client-side routes; fall back to index.html for any
    # path that isn't a real static file so deep links and refreshes work.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
